!!! 5
html(lang='en')
head
    title= 'Just Anguled!!!'
    include includes/meta
    include includes/styles
  body(ng-app='trackr')
    div(ng-controller='wrapperController')
        include includes/menus
        div.container.container-body
            div(ng-view)
            include includes/scripts
            trackr-notification(
                uuid='notify.uuid', message='notify.message', type='notify.type', timeout='notify.timeout'
            )